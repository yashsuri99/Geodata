Using the Google Places API with a Database and
Visualizing Data on Google Map

In this project, we have a file data which conatins the name of universities of all over the world along with there longitude and latitudes
then when we run the geoload.py all the data read from where.data is put in an sqlpite file 
after that when we run geodump.py then using the Google geocoding API
to clean up some user-entered geographic locations ofuniversity names and then placing the data on a Google Map
an html link is created which is the link to the google map marking all the locations on the map given to us.
